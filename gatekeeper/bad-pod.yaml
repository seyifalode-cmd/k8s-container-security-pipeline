apiVersion: v1
kind: Pod
metadata:
  name: bad-pod
  namespace: default
spec:
  containers:
  - name: bad-container
    image: nginx
    securityContext:
      runAsUser: 0  # ROOT USER — blocked by Gatekeeper policy
